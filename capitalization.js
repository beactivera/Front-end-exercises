const str1 = "peTER";
let firstL = str1.substring(0, 1);
let otherL = str1.substring(1, str1.length);

let changedStr = firstL.toUpperCase() + otherL.toLowerCase();

console.log(changedStr);
// let strFinal = str1.indexOf()
